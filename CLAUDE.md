# CLAUDE.md

See [`AGENTS.md`](AGENTS.md) — that file is the source of truth for agent instructions in this repository,
kept as a single file so instructions don't drift between tool-specific copies. This file exists only so
tooling that specifically looks for `CLAUDE.md` finds a pointer rather than nothing.
